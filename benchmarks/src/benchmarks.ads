package Benchmarks is

   type Benchmark_Type is tagged private;


   function Benchmark (
      Process : not null access procedure;
      Timeout : in Duration)
      return 

private

end Benchmarks;
